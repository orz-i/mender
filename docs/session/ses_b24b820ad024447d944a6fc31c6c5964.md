# Session：Reviewed OAuth Refresh Alpha

**Session id:** ses_b24b820ad024447d944a6fc31c6c5964
**Created:** unix:1789392868
**Updated:** unix:1789395866
**Status:** active
**Host session scope:** host-session:f469a465ece78120da5de60df27407ac2fb02c786dfd8fc9bd42ee7a3f475fab
**Parent session id:** ses_ca840dfb85e2495e94c8037401927c36

## 用户核心目标

- 补齐 G3/T07-T08 的 Reviewed OAuth Refresh Alpha。保持现有 OAuth 授权开始/callback/revoke 行为兼容，新增显式、默认关闭的刷新能力：初始授权在开启 refresh 时要求服务端安全保存 refresh token 与 provider scope 事实；后台 refresh runtime 只处理 reviewed provider，使用独立 connection-manager authority 与 mounted Secret Vault，按 Connection revision CAS 竞争收敛，任何 loser 不得覆盖 winner；刷新后 Access Token 使用新的 immutable credential ref/revision，旧 token 不再被 runtime Connection 引用；provider 返回 scope 缩减、invalid_grant 或不可接受 token 时 fail closed，将 Connection 标记为 error/不可执行，而瞬时网络错误保持 active 供后续重试；refresh token rotation 必须安全处理，provider 未轮换时可继续使用当前 refresh secret；浏览器永远不能读取 access/refresh token，也不能选择 token endpoint/provider。补真实 PostgreSQL + local token endpoint integration，证明并发刷新单赢家、scope shrink/invalid_grant、revoke、RLS/最小权限和 runtime 使用新 access token。最后更新 G3 evidence，只在自动化证据通过后把 T07/T08 标为 covered。每个 slice 独立 commit，最终完整门禁；不 push、不 deploy。

## 已确认事实

- 自动阶段检查点：tool=apply_patch, status=completed, success=true
- summary=M backend/internal/bootstrap/worker.go
M backend/internal/bootstrap/reviewed_worker_host.go
M backend/internal/bootstrap/runtime_host_test.go
- 自动阶段检查点：tool=exec_command, status=succeeded, success=true
- command=node scripts/backend.mjs fmt ./internal/bootstrap/connection_oauth_config_test.go ./internal/bootstrap/reviewed_worker_host.go ./internal/bootstrap/reviewed_worker_host_test.go ./internal/bootstrap/runtime_host_test.go ./internal/bootstrap/worker.go ./internal/bootstrap/operator.go ./internal/bootstrap/persistence.go
- 自动阶段检查点：tool=stage_commit, status=completed, success=true
- command=pnpm check:workspace
- command=pnpm test:integration:docker

## 已完成修改

- backend/internal/bootstrap/worker.go
- backend/internal/bootstrap/reviewed_worker_host.go
- backend/internal/bootstrap/runtime_host_test.go
- backend/internal/bootstrap/connection_oauth_config_test.go
- backend/internal/bootstrap/reviewed_worker_host_test.go

## 关键设计决定


## 测试结果

- verification_kind=format, success=true
- verification_kind=check, success=true
- verification_kind=test, success=true

## 当前运行状态

- task_id=0efd79031d7e422fb48f01aec8d4c743
- task_status=active
- tool=exec_command
- session_id="169c768f-7715-420f-95ad-8e1ee22bd0d1"
- execution_status="succeeded"
- exit_code=0
- last_output_at="2026-09-14T14:24:24.374Z"
- branch=main
- head=31ce093582328ce6e8c0d9fb8b1f5fe328e13e4e
- baseline_matches=Some(true)

## 剩余问题


## 下一步

- Slice 1: add OAuth token/refresh metadata contract, immutable secret addressing and least-privilege persistence
- Slice 2: add reviewed refresh provider/runtime with CAS concurrency, scope-shrink and invalid_grant fail-closed integration
- Slice 3: wire explicit default-off bootstrap/config/operator evidence and extend G3 T07/T08 evidence package
- Run oauth refresh evidence, pnpm check, full Go race, isolated PostgreSQL integration, git diff --check
- Strict complete_work_session and leave working tree clean

## 本轮检查点

### auto-apply_patch-82ede25a7868a909

```json
{
  "turn_id": "auto-apply_patch-82ede25a7868a909",
  "timestamp": "unix:1789395718",
  "user_intent": "补齐 G3/T07-T08 的 Reviewed OAuth Refresh Alpha。保持现有 OAuth 授权开始/callback/revoke 行为兼容，新增显式、默认关闭的刷新能力：初始授权在开启 refresh 时要求服务端安全保存 refresh token 与 provider scope 事实；后台 refresh runtime 只处理 reviewed provider，使用独立 connection-manager authority 与 mounted Secret Vault，按 Connection revision CAS 竞争收敛，任何 loser 不得覆盖 winner；刷新后 Access Token 使用新的 immutable credential ref/revision，旧 token 不再被 runtime Connection 引用；provider 返回 scope 缩减、invalid_grant 或不可接受 token 时 fail closed，将 Connection 标记为 error/不可执行，而瞬时网络错误保持 active 供后续重试；refresh token rotation 必须安全处理，provider 未轮换时可继续使用当前 refresh secret；浏览器永远不能读取 access/refresh token，也不能选择 token endpoint/provider。补真实 PostgreSQL + local token endpoint integration，证明并发刷新单赢家、scope shrink/invalid_grant、revoke、RLS/最小权限和 runtime 使用新 access token。最后更新 G3 evidence，只在自动化证据通过后把 T07/T08 标为 covered。每个 slice 独立 commit，最终完整门禁；不 push、不 deploy。",
  "findings": [
    "自动阶段检查点：tool=apply_patch, status=completed, success=true",
    "summary=M backend/internal/bootstrap/worker.go\nM backend/internal/bootstrap/reviewed_worker_host.go\nM backend/internal/bootstrap/runtime_host_test.go"
  ],
  "decisions": [],
  "files_changed": [
    "backend/internal/bootstrap/worker.go",
    "backend/internal/bootstrap/reviewed_worker_host.go",
    "backend/internal/bootstrap/runtime_host_test.go"
  ],
  "tests": [],
  "runtime_state": [
    "task_id=0efd79031d7e422fb48f01aec8d4c743",
    "task_status=active",
    "tool=apply_patch",
    "branch=main",
    "head=31ce093582328ce6e8c0d9fb8b1f5fe328e13e4e",
    "baseline_matches=Some(true)"
  ],
  "remaining_issues": [],
  "next_actions": [
    "Slice 1: add OAuth token/refresh metadata contract, immutable secret addressing and least-privilege persistence",
    "Slice 2: add reviewed refresh provider/runtime with CAS concurrency, scope-shrink and invalid_grant fail-closed integration",
    "Slice 3: wire explicit default-off bootstrap/config/operator evidence and extend G3 T07/T08 evidence package",
    "Run oauth refresh evidence, pnpm check, full Go race, isolated PostgreSQL integration, git diff --check",
    "Strict complete_work_session and leave working tree clean"
  ],
  "notes": "Anchor 自动保存的结构化阶段检查点；相同阶段身份会幂等更新。"
}
```

### auto-exec_command-502e7441466835d2

```json
{
  "turn_id": "auto-exec_command-502e7441466835d2",
  "timestamp": "unix:1789395741",
  "user_intent": "补齐 G3/T07-T08 的 Reviewed OAuth Refresh Alpha。保持现有 OAuth 授权开始/callback/revoke 行为兼容，新增显式、默认关闭的刷新能力：初始授权在开启 refresh 时要求服务端安全保存 refresh token 与 provider scope 事实；后台 refresh runtime 只处理 reviewed provider，使用独立 connection-manager authority 与 mounted Secret Vault，按 Connection revision CAS 竞争收敛，任何 loser 不得覆盖 winner；刷新后 Access Token 使用新的 immutable credential ref/revision，旧 token 不再被 runtime Connection 引用；provider 返回 scope 缩减、invalid_grant 或不可接受 token 时 fail closed，将 Connection 标记为 error/不可执行，而瞬时网络错误保持 active 供后续重试；refresh token rotation 必须安全处理，provider 未轮换时可继续使用当前 refresh secret；浏览器永远不能读取 access/refresh token，也不能选择 token endpoint/provider。补真实 PostgreSQL + local token endpoint integration，证明并发刷新单赢家、scope shrink/invalid_grant、revoke、RLS/最小权限和 runtime 使用新 access token。最后更新 G3 evidence，只在自动化证据通过后把 T07/T08 标为 covered。每个 slice 独立 commit，最终完整门禁；不 push、不 deploy。",
  "findings": [
    "自动阶段检查点：tool=exec_command, status=succeeded, success=true",
    "command=node scripts/backend.mjs fmt ./internal/bootstrap/connection_oauth_config_test.go ./internal/bootstrap/reviewed_worker_host.go ./internal/bootstrap/reviewed_worker_host_test.go ./internal/bootstrap/runtime_host_test.go ./internal/bootstrap/worker.go ./internal/bootstrap/operator.go ./internal/bootstrap/persistence.go"
  ],
  "decisions": [],
  "files_changed": [
    "backend/internal/bootstrap/connection_oauth_config_test.go",
    "backend/internal/bootstrap/reviewed_worker_host_test.go"
  ],
  "tests": [
    "verification_kind=format, success=true"
  ],
  "runtime_state": [
    "task_id=0efd79031d7e422fb48f01aec8d4c743",
    "task_status=active",
    "tool=exec_command",
    "session_id=\"8e87936e-e041-48e1-85c1-8bd84f77be0e\"",
    "execution_status=\"succeeded\"",
    "exit_code=0",
    "last_output_at=\"2026-09-14T14:22:20.013Z\"",
    "branch=main",
    "head=31ce093582328ce6e8c0d9fb8b1f5fe328e13e4e",
    "baseline_matches=Some(true)"
  ],
  "remaining_issues": [],
  "next_actions": [
    "Slice 1: add OAuth token/refresh metadata contract, immutable secret addressing and least-privilege persistence",
    "Slice 2: add reviewed refresh provider/runtime with CAS concurrency, scope-shrink and invalid_grant fail-closed integration",
    "Slice 3: wire explicit default-off bootstrap/config/operator evidence and extend G3 T07/T08 evidence package",
    "Run oauth refresh evidence, pnpm check, full Go race, isolated PostgreSQL integration, git diff --check",
    "Strict complete_work_session and leave working tree clean"
  ],
  "notes": "Anchor 自动保存的结构化阶段检查点；相同阶段身份会幂等更新。"
}
```

### auto-8dd162f6bcd3af09

```json
{
  "turn_id": "auto-8dd162f6bcd3af09",
  "timestamp": "unix:1789393797",
  "user_intent": "",
  "findings": [],
  "decisions": [],
  "files_changed": [],
  "tests": [],
  "runtime_state": [],
  "remaining_issues": [],
  "next_actions": [],
  "notes": ""
}
```

### auto-stage_commit-5f238dc67f4e9471

```json
{
  "turn_id": "auto-stage_commit-5f238dc67f4e9471",
  "timestamp": "unix:1789393798",
  "user_intent": "补齐 G3/T07-T08 的 Reviewed OAuth Refresh Alpha。保持现有 OAuth 授权开始/callback/revoke 行为兼容，新增显式、默认关闭的刷新能力：初始授权在开启 refresh 时要求服务端安全保存 refresh token 与 provider scope 事实；后台 refresh runtime 只处理 reviewed provider，使用独立 connection-manager authority 与 mounted Secret Vault，按 Connection revision CAS 竞争收敛，任何 loser 不得覆盖 winner；刷新后 Access Token 使用新的 immutable credential ref/revision，旧 token 不再被 runtime Connection 引用；provider 返回 scope 缩减、invalid_grant 或不可接受 token 时 fail closed，将 Connection 标记为 error/不可执行，而瞬时网络错误保持 active 供后续重试；refresh token rotation 必须安全处理，provider 未轮换时可继续使用当前 refresh secret；浏览器永远不能读取 access/refresh token，也不能选择 token endpoint/provider。补真实 PostgreSQL + local token endpoint integration，证明并发刷新单赢家、scope shrink/invalid_grant、revoke、RLS/最小权限和 runtime 使用新 access token。最后更新 G3 evidence，只在自动化证据通过后把 T07/T08 标为 covered。每个 slice 独立 commit，最终完整门禁；不 push、不 deploy。",
  "findings": [
    "自动阶段检查点：tool=stage_commit, status=completed, success=true"
  ],
  "decisions": [],
  "files_changed": [],
  "tests": [],
  "runtime_state": [
    "task_id=0efd79031d7e422fb48f01aec8d4c743",
    "task_status=active",
    "tool=stage_commit",
    "commit_sha=\"a3b9d7c7c1a6b38cd61b4f8b4ab41dfa3d17d760\"",
    "branch=main",
    "head=a3b9d7c7c1a6b38cd61b4f8b4ab41dfa3d17d760",
    "baseline_matches=Some(false)"
  ],
  "remaining_issues": [],
  "next_actions": [
    "Slice 1: add OAuth token/refresh metadata contract, immutable secret addressing and least-privilege persistence",
    "Slice 2: add reviewed refresh provider/runtime with CAS concurrency, scope-shrink and invalid_grant fail-closed integration",
    "Slice 3: wire explicit default-off bootstrap/config/operator evidence and extend G3 T07/T08 evidence package",
    "Run oauth refresh evidence, pnpm check, full Go race, isolated PostgreSQL integration, git diff --check",
    "Strict complete_work_session and leave working tree clean"
  ],
  "notes": "Anchor 自动保存的结构化阶段检查点；相同阶段身份会幂等更新。"
}
```

### auto-stage_commit-e7d94ff90e9c0d36

```json
{
  "turn_id": "auto-stage_commit-e7d94ff90e9c0d36",
  "timestamp": "unix:1789394872",
  "user_intent": "补齐 G3/T07-T08 的 Reviewed OAuth Refresh Alpha。保持现有 OAuth 授权开始/callback/revoke 行为兼容，新增显式、默认关闭的刷新能力：初始授权在开启 refresh 时要求服务端安全保存 refresh token 与 provider scope 事实；后台 refresh runtime 只处理 reviewed provider，使用独立 connection-manager authority 与 mounted Secret Vault，按 Connection revision CAS 竞争收敛，任何 loser 不得覆盖 winner；刷新后 Access Token 使用新的 immutable credential ref/revision，旧 token 不再被 runtime Connection 引用；provider 返回 scope 缩减、invalid_grant 或不可接受 token 时 fail closed，将 Connection 标记为 error/不可执行，而瞬时网络错误保持 active 供后续重试；refresh token rotation 必须安全处理，provider 未轮换时可继续使用当前 refresh secret；浏览器永远不能读取 access/refresh token，也不能选择 token endpoint/provider。补真实 PostgreSQL + local token endpoint integration，证明并发刷新单赢家、scope shrink/invalid_grant、revoke、RLS/最小权限和 runtime 使用新 access token。最后更新 G3 evidence，只在自动化证据通过后把 T07/T08 标为 covered。每个 slice 独立 commit，最终完整门禁；不 push、不 deploy。",
  "findings": [
    "自动阶段检查点：tool=stage_commit, status=completed, success=true"
  ],
  "decisions": [],
  "files_changed": [],
  "tests": [],
  "runtime_state": [
    "task_id=0efd79031d7e422fb48f01aec8d4c743",
    "task_status=active",
    "tool=stage_commit",
    "commit_sha=\"31ce093582328ce6e8c0d9fb8b1f5fe328e13e4e\"",
    "branch=main",
    "head=31ce093582328ce6e8c0d9fb8b1f5fe328e13e4e",
    "baseline_matches=Some(true)"
  ],
  "remaining_issues": [],
  "next_actions": [
    "Slice 1: add OAuth token/refresh metadata contract, immutable secret addressing and least-privilege persistence",
    "Slice 2: add reviewed refresh provider/runtime with CAS concurrency, scope-shrink and invalid_grant fail-closed integration",
    "Slice 3: wire explicit default-off bootstrap/config/operator evidence and extend G3 T07/T08 evidence package",
    "Run oauth refresh evidence, pnpm check, full Go race, isolated PostgreSQL integration, git diff --check",
    "Strict complete_work_session and leave working tree clean"
  ],
  "notes": "Anchor 自动保存的结构化阶段检查点；相同阶段身份会幂等更新。"
}
```

### auto-exec_command-96801ff6b6b38bb0

```json
{
  "turn_id": "auto-exec_command-96801ff6b6b38bb0",
  "timestamp": "unix:1789395826",
  "user_intent": "补齐 G3/T07-T08 的 Reviewed OAuth Refresh Alpha。保持现有 OAuth 授权开始/callback/revoke 行为兼容，新增显式、默认关闭的刷新能力：初始授权在开启 refresh 时要求服务端安全保存 refresh token 与 provider scope 事实；后台 refresh runtime 只处理 reviewed provider，使用独立 connection-manager authority 与 mounted Secret Vault，按 Connection revision CAS 竞争收敛，任何 loser 不得覆盖 winner；刷新后 Access Token 使用新的 immutable credential ref/revision，旧 token 不再被 runtime Connection 引用；provider 返回 scope 缩减、invalid_grant 或不可接受 token 时 fail closed，将 Connection 标记为 error/不可执行，而瞬时网络错误保持 active 供后续重试；refresh token rotation 必须安全处理，provider 未轮换时可继续使用当前 refresh secret；浏览器永远不能读取 access/refresh token，也不能选择 token endpoint/provider。补真实 PostgreSQL + local token endpoint integration，证明并发刷新单赢家、scope shrink/invalid_grant、revoke、RLS/最小权限和 runtime 使用新 access token。最后更新 G3 evidence，只在自动化证据通过后把 T07/T08 标为 covered。每个 slice 独立 commit，最终完整门禁；不 push、不 deploy。",
  "findings": [
    "自动阶段检查点：tool=exec_command, status=succeeded, success=true",
    "command=pnpm check:workspace"
  ],
  "decisions": [],
  "files_changed": [],
  "tests": [
    "verification_kind=check, success=true"
  ],
  "runtime_state": [
    "task_id=0efd79031d7e422fb48f01aec8d4c743",
    "task_status=active",
    "tool=exec_command",
    "session_id=\"3e91ff13-21a9-4250-91bc-f8dfe49de2e6\"",
    "execution_status=\"succeeded\"",
    "exit_code=0",
    "last_output_at=\"2026-09-14T14:23:44.792Z\"",
    "branch=main",
    "head=31ce093582328ce6e8c0d9fb8b1f5fe328e13e4e",
    "baseline_matches=Some(true)"
  ],
  "remaining_issues": [],
  "next_actions": [
    "Slice 1: add OAuth token/refresh metadata contract, immutable secret addressing and least-privilege persistence",
    "Slice 2: add reviewed refresh provider/runtime with CAS concurrency, scope-shrink and invalid_grant fail-closed integration",
    "Slice 3: wire explicit default-off bootstrap/config/operator evidence and extend G3 T07/T08 evidence package",
    "Run oauth refresh evidence, pnpm check, full Go race, isolated PostgreSQL integration, git diff --check",
    "Strict complete_work_session and leave working tree clean"
  ],
  "notes": "Anchor 自动保存的结构化阶段检查点；相同阶段身份会幂等更新。"
}
```

### auto-exec_command-7263ad76ae774e66

```json
{
  "turn_id": "auto-exec_command-7263ad76ae774e66",
  "timestamp": "unix:1789395866",
  "user_intent": "补齐 G3/T07-T08 的 Reviewed OAuth Refresh Alpha。保持现有 OAuth 授权开始/callback/revoke 行为兼容，新增显式、默认关闭的刷新能力：初始授权在开启 refresh 时要求服务端安全保存 refresh token 与 provider scope 事实；后台 refresh runtime 只处理 reviewed provider，使用独立 connection-manager authority 与 mounted Secret Vault，按 Connection revision CAS 竞争收敛，任何 loser 不得覆盖 winner；刷新后 Access Token 使用新的 immutable credential ref/revision，旧 token 不再被 runtime Connection 引用；provider 返回 scope 缩减、invalid_grant 或不可接受 token 时 fail closed，将 Connection 标记为 error/不可执行，而瞬时网络错误保持 active 供后续重试；refresh token rotation 必须安全处理，provider 未轮换时可继续使用当前 refresh secret；浏览器永远不能读取 access/refresh token，也不能选择 token endpoint/provider。补真实 PostgreSQL + local token endpoint integration，证明并发刷新单赢家、scope shrink/invalid_grant、revoke、RLS/最小权限和 runtime 使用新 access token。最后更新 G3 evidence，只在自动化证据通过后把 T07/T08 标为 covered。每个 slice 独立 commit，最终完整门禁；不 push、不 deploy。",
  "findings": [
    "自动阶段检查点：tool=exec_command, status=succeeded, success=true",
    "command=pnpm test:integration:docker"
  ],
  "decisions": [],
  "files_changed": [],
  "tests": [
    "verification_kind=test, success=true"
  ],
  "runtime_state": [
    "task_id=0efd79031d7e422fb48f01aec8d4c743",
    "task_status=active",
    "tool=exec_command",
    "session_id=\"169c768f-7715-420f-95ad-8e1ee22bd0d1\"",
    "execution_status=\"succeeded\"",
    "exit_code=0",
    "last_output_at=\"2026-09-14T14:24:24.374Z\"",
    "branch=main",
    "head=31ce093582328ce6e8c0d9fb8b1f5fe328e13e4e",
    "baseline_matches=Some(true)"
  ],
  "remaining_issues": [],
  "next_actions": [
    "Slice 1: add OAuth token/refresh metadata contract, immutable secret addressing and least-privilege persistence",
    "Slice 2: add reviewed refresh provider/runtime with CAS concurrency, scope-shrink and invalid_grant fail-closed integration",
    "Slice 3: wire explicit default-off bootstrap/config/operator evidence and extend G3 T07/T08 evidence package",
    "Run oauth refresh evidence, pnpm check, full Go race, isolated PostgreSQL integration, git diff --check",
    "Strict complete_work_session and leave working tree clean"
  ],
  "notes": "Anchor 自动保存的结构化阶段检查点；相同阶段身份会幂等更新。"
}
```

