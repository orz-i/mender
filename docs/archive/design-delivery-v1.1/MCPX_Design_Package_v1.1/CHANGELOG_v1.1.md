# v1.1 变更记录

基线日期：2026-09-04。用户确定前端 pnpm、DDD、清洁架构与依赖倒置。

## 架构与工程

A03 改为按限界上下文纵向组织，再在上下文内分层；删除全局 domain／application 与 modules 并存的混合目录。明确消费方端口、public 合同、防腐层、数据所有者与装配根。

A04、A05、A13 明确聚合与写表归属：Provider 主记录归 supply，价格版本归 commerce；通过受控同库事务保留 Run／预留／任务原子性，新增 ADR-020，待 G0 评审。

A14 固定 pnpm 根工作区、单锁、workspace: 和冻结安装；前端按用户任务组织领域模块，domain／application 不依赖 React、Router、Query 或生成客户端。去除全局 features 万能包。

新增 G00–G10 专项规范、两张图和 engineering/ 政策／模板。

## 任务与验收

92 个任务中 20 项强化交付和验收；需求 34→40，测试 40→48，ADR 16→20，上线检查 25→30。Excel 10→12 表，新增 DDD上下文及工程治理（16 条规则）。原 18 项风险保留。

原 20 周／261.25 人日仅保留为待 G0 重新校准的原估算。未提供现有代码，未实施迁移或估算实际迁移人日；不能解读为新增约束零成本。

## 未改变与未实施

Go＋Gin、React＋Vite＋Tailwind＋shadcn＋React Router 保持不变。API＋Worker 起步、声明式插件优先和 S6 隔离运行时独立准入不变。原 contracts/ 的业务字段及示例不因工程分层而擅自升级。

已确定的是方法和包管理器，精确版本、具体上下文边界及事务例外仍需评审。政策文件未连接业务 CI，没有真实应用构建／集成／安全／性能测试结果。
