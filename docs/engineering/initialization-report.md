# Mender 初始化报告

日期：2026-09-08。范围：根据设计交付 v1.1 完成项目初始化和文档归档，项目名统一为 Mender。基于既有 `D:\mender` 仓库工作，保留原 MIT 许可证和 `.gitignore` 中的 `.anchor/`。初始化验收时尚未提交或推送，后续提交以 Git 历史为准；未部署。

## 已建立的内容

- 根 pnpm workspace、精确 Node／pnpm／Go 清单、真实前后端依赖锁、EditorConfig、Git 换行和忽略规则。
- Go API／Worker、显式 bootstrap、8 个候选上下文及其层边界；API 提供存活和业务未就绪探针，Worker 空闲等待终止。
- Console／Admin 两个 React／Vite 入口，Tailwind 和共享 shadcn UI；首页、真实健康请求、重试与未知路由恢复。
- 共享 UI、健康传输 API client 与 TypeScript 配置包；应用内部 service-status 模块演示消费方端口与传输映射。
- 基础 GitHub Actions 工作流、项目工作约定和 PR 模板。工作流中的第三方 Actions 引用使用已核对的提交摘要。
- 49 个原包清单文件及原 SHA256SUMS.txt 保真归档；3 份 Mender Markdown 设计文档、7 对配图、完整 JSON 计划、合同和工程模板。
- 当前文档索引、开发指南、更名映射、工程决策和后续实施顺序。

## 验证记录

执行环境：Windows、PowerShell；Node `24.19.0`、pnpm `11.18.0`、Go `1.26.7 windows/amd64`。下表为本次实际执行结果；不将本地结果推断为远程 CI 或生产结果。

| 检查 | 状态 |
| --- | --- |
| 原包 SHA-256 | 已核对 49／49，原始摘要列表另行固定 |
| Go 测试／vet／构建 | 通过；探针语义、未开放业务路由、Worker 取消和 API 启动失败有测试。空上下文包不计业务覆盖 |
| 健康传输测试 | 4 项通过：成功、HTTP 失败、非法响应、取消 |
| `pnpm install --frozen-lockfile` | 通过；单一真实 pnpm 锁文件，无发布时间豁免 |
| `pnpm check` | 通过；工具链、5 个工作区包、文档、合同、lint、类型、测试和构建全部成功 |
| `pnpm check:docs` | 49 个原始摘要、计划记录数量／任务引用及 63 个当前 Markdown 本地链接通过 |
| `pnpm check:contracts` | Schema、样例和制品摘要通过；OpenAPI 10 条路径／11 个操作结构通过 |
| 端口与样式调整后的定向复核 | Go test／vet／build、两端 typecheck＋Vite build、文档及工作区检查通过 |
| API 真实 HTTP 请求 | `/healthz` 返回 200；`/readyz` 返回预期的 503；`/v1/runs` 返回 404 |
| Console 浏览器验证 | 首页和状态页可用；先验证 API 未启动时的失败提示，启动后点击重试成功；未知路由可返回首页 |
| Admin 浏览器验证 | 独立首页与状态页可用；键盘 Enter 激活连接入口，成功读取真实 API；未见浏览器脚本警告／错误 |
| 响应式复核 | 桌面 Console 和 390px 窄屏 Admin 已查看；测得页面 scrollWidth 与 clientWidth 相等，无横向溢出；修正了链接按钮的文字对比度 |

本机 `127.0.0.1:8080` 绑定返回 WinError 10048；核验 `18080` 可用后，将 API、两端代理、环境示例和启动文档统一为 `127.0.0.1:18080`。没有停止其他项目的进程或修改系统网络设置。

用于验证的本地 API 和 Vite 进程已停止；按开发指南即可重新启动。浏览器临时视口设置已恢复。

## 与原计划的对应关系

| 原任务 | 本次提供 | 尚未满足的验收条件 |
| --- | --- | --- |
| S0-02／S0-10 | 候选上下文目录、映射和工具链记录 | 统一语言评审、实际负责人、G0 决议、重估、ADR-020 |
| S1-01 | Go 上下文分层、API／Worker 与显式装配 | 真实领域用例、小业务端口及 T42／T46 完整证据 |
| S1-07 | pnpm 单锁、双入口构建、共享 UI、前端端口示例 | 真实用户任务、全量依赖边界及 T44／T46 |
| S1-13 | 冻结安装、基础工具链／工作区检查、CI 定义 | 导入图负向 fixture、生成漂移、秘密扫描和例外到期门禁 |
| S1-14 | 本地配置示例和探针 | 数据库、对象存储、预发布环境、认证入口与监控 |

上述任务只获得部分实施证据，没有修改原 WBS 的“未开始”状态或填入完成百分比。初始化单独在本报告记账，G0–G5、ADR-020 与 T41–T48 的完整验收均未宣称通过。

## 交付边界

没有实现登录、OIDC、租户隔离、目录、API Key、Run、可靠任务队列、MCP／Agent 适配、支付计费、审计或数据库迁移。`/readyz` 维持 `503`；合同样例中的业务 API 仍为 `404`。

文档检查覆盖原包字节、记录数量、任务引用和本地 Markdown 链接；没有重新渲染 DOCX／PDF、重算 Excel 或复核全部外部来源。原二进制制品保留 MCPX 名称，当前可编辑文档使用 Mender。合同 wire 标识暂保留，详见 [更名记录](../decisions/0001-project-name.md)。

完整 Go／TS 依赖图、符号级 I/O、跨域写表、事务回滚、权限／资金／安全／协议／性能验证、远程 CI 与生产部署均待后续实施。当前入口与视觉样式是初始化预览，不代表全部设计交互已经落地。
